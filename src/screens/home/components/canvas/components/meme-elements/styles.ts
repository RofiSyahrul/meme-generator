import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  elementSelected: {
    borderRadius: 4,
    shadowOffset: {
      width: 0,
      height: 0.5,
    },
    shadowOpacity: 0.01,
    shadowRadius: 1,
    elevation: 1,
  },
});
